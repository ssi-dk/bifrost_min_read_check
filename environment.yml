channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - snakemake-minimal==5.31.1 # updated from 5.7.1 because of envvars
  - bbmap==38.58
  - pip
  - pip:
    - pytest
    - pytest-cov
    - pytest-profiling
    - coverage
    - pyyaml
    - argh
    - watchdog
    - bifrostlib
    - -e .